export enum ExchangeType {
  BINANCE = 'BINANCE',
  BITMART = 'BITMART',
  ALL = 'ALL',
}

export enum LimitType {
  MIN = 1,
  MAX = 100,
  DEFAULT = 20,
}

export const DefaultLimit = 20;

export enum SortType {
  ASC = 'ASC',
  DESC = 'DESC',
}
